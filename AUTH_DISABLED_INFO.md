# ⚠️ Временное отключение авторизации

## Статус: ОТКЛЮЧЕНА

Проверка авторизации временно отключена для тестирования и разработки. Все пользователи имеют полный доступ ко всем функциям платформы.

## Что изменено

### 1. dashboard.html
- ✅ Отключена проверка `checkAuth()` с редиректом на login
- ✅ Используются демо-данные для неавторизованных пользователей
- ✅ Гостевой режим: "Гость Демо" (demo@example.com)

### 2. courses.html
- ✅ Отключена блокировка доступа к курсам
- ✅ Все курсы доступны без авторизации
- ✅ Условие изменено на `if (false && !user)` - никогда не выполнится

### 3. auth.js
- ✅ Добавлен комментарий о временном отключении
- ✅ Функция `checkAuth()` работает как обычно
- ✅ Просто не используется для блокировки доступа

### 4. quiz-cards.html
- ✅ Изначально не имел проверки авторизации
- ✅ Доступен всем пользователям

## Как включить обратно

### Вариант 1: Быстрое включение

**dashboard.html:**
```javascript
// Раскомментировать эти строки:
if (!user) {
  alert('Пожалуйста, войдите в систему');
  window.location.href = 'login.html';
}
```

**courses.html:**
```javascript
// Изменить условие с:
if (false && !user) {

// На:
if (!user) {
```

### Вариант 2: Через поиск

Найти все комментарии `TEMPORARILY DISABLED` и раскомментировать код проверки.

## Файлы с изменениями

```
✓ auth.js - добавлен комментарий
✓ dashboard.html - отключена проверка + демо данные
✓ courses.html - отключена блокировка курсов
✓ quiz-cards.html - без изменений (не было проверки)
```

## Демо-пользователь

Когда авторизация отключена, используются следующие данные:

```javascript
{
  firstName: 'Гость',
  lastName: 'Демо',
  email: 'demo@example.com'
}
```

## Безопасность

⚠️ **ВАЖНО:** Это только для разработки и тестирования!

Перед деплоем на продакшн:
1. Включите проверку авторизации обратно
2. Проверьте все защищённые страницы
3. Убедитесь что server.js корректно проверяет токены
4. Протестируйте flow регистрации/входа

## Что работает без авторизации

✅ Главная страница (index.html)
✅ О нас (about.html)
✅ Блог (blog.html)
✅ FAQ (faq.html)
✅ Помощь (help.html)
✅ Карьера (career.html)
✅ Курсы (courses.html) - ВРЕМЕННО
✅ Личный кабинет (dashboard.html) - ВРЕМЕННО
✅ Карточки вопросов (quiz-cards.html)
✅ React приложение (dispatcher-cards-app)

## Что требует авторизации (когда включена)

🔒 Личный кабинет (dashboard.html)
🔒 Доступ к курсам (courses.html)
🔒 Сертификаты
🔒 Прогресс обучения
🔒 Сохранение результатов

## Дата отключения

23 февраля 2026

## Причина

Разработка и тестирование нового функционала (React приложение с карточками)

---

**Не забудьте включить авторизацию перед продакшн деплоем!**
